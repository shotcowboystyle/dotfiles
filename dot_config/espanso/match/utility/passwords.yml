# matches:
#   # Hash a password string via a docker container
#   - trigger: ";dhashpw"
#     replace: "docker run -it --rm alpine mkpasswd --method=sha-512 {{clipboard}}"
#     vars:
#       - name: "clipboard"
#         type: "clipboard"
